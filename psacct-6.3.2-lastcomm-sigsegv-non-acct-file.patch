diff --git a/file_rd.c b/file_rd.c
index 8840319..9e535a7 100644
--- a/file_rd.c
+++ b/file_rd.c
@@ -122,6 +122,10 @@ file_reader_get_entry (struct file_rd_info *fri)
 			      ? max_recs
 			      : fri->buffered_records);
 	      
+	      if (!recs_to_read)
+	        goto no_more_records;
+
+
 	      /* Move back in the file */
 
 	      fseek (fri->fp, -fri->record_size * recs_to_read,
